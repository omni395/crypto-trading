<nav class="bg-gray-700 border-gray-900">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
    <a href="<%= root_path %>" class="flex items-center space-x-3">
      <i class="fa-solid fa-coins text-2xl text-green-700 mx-4"></i>
      <span class="self-center text-2xl font-semibold whitespace-nowrap text-green-700">Crypto Trading</span>
    </a>
    <ul class="bg-gray-700 font-medium flex flex-row p-0 mt-0 border-0 rounded-lg gap-4 items-center">
      <li>
        <%= link_to 'Главная', root_path, class: 'block py-2 px-3 text-white rounded-sm hover:bg-gray-600' %>
      </li>
      <li>
        <%= link_to 'Пусто', root_path, class: 'block py-2 px-3 text-white rounded-sm hover:bg-gray-600' %>
      </li>
      <li>
        <%= link_to 'Пусто', root_path, class: 'block py-2 px-3 text-white rounded-sm hover:bg-gray-600' %>
      </li>
      <% if user_signed_in? %>
        <li>
          <button id="settings-btn" type="button"
            data-modal-target="vue-settings-modal"
            data-modal-toggle="vue-settings-modal"
            class="flex items-center justify-center p-2 rounded hover:bg-gray-600 focus:outline-none cursor-pointer">
            <i class="fa-solid fa-gear text-xl text-gray-200"></i>
          </button>
        </li>
        <li>
          <div id="vue-user-dropdown" data-admin="<%= policy(:admin).admin? %>"></div>
        </li>
      <% else %>
        <li>
          <%= link_to 'Вход', new_user_session_path, class: 'py-2 px-3 text-white rounded-sm hover:bg-gray-600' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
<div id="vue-settings-modal" data-admin="<%= policy(:admin).admin? %>"></div>